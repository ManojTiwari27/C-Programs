#include<stdio.h>
void iseven(num)
{
    if (num&1)
        printf("Odd Number");
    else
        printf("Even number");
}
void main()
{
    int num;
    printf("Enter a number to Check whether even or odd\n");
    scanf("%d",&num);
    iseven(num);
}
