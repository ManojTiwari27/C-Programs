#include<stdio.h>
void main()
{
    int i,n,r=1;
    printf("Enter a number  ");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        r = r*i;
    }
      printf("Sum of first n natural number is %d\n",r);
}
